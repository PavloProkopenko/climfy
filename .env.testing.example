# Playwright E2E test credentials
# Create this user in Supabase using the SQL in tests/resources/setup.sql
# The user must have onboarding_completed = true in user_preferences
PLAYWRIGHT_TEST_EMAIL=test@example.com
PLAYWRIGHT_TEST_PASSWORD=your-test-password
